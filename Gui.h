#pragma once
#include <string>
#include <iostream>
#include <fstream>
#include <string>

class Gui
{
public:

	static void menu(void);
	static std::string selectMenuOptions(void);
	static void howToPlay(void);
	static int selectCategory(void);
};

